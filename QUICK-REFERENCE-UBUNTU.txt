╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║       🏥  RECONCILIA - UBUNTU/WSL QUICK REFERENCE  🏥        ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│  📥 INSTALLATION (One-Time Setup)                            │
└───────────────────────────────────────────────────────────────┘

1. Open Terminal (Ubuntu) or WSL
2. Download script:
   curl -fsSL https://raw.githubusercontent.com/khalidmihlar/\
   reconcilia-app/main/setup-reconcilia-ubuntu.sh -o setup.sh

3. Make executable and run:
   chmod +x setup.sh
   ./setup.sh

4. Wait 5-10 minutes
5. Press Enter when prompted


┌───────────────────────────────────────────────────────────────┐
│  🚀 STARTING THE APP                                         │
└───────────────────────────────────────────────────────────────┘

METHOD 1: Launch Script
  → cd reconcilia-app
  → ./start-reconcilia.sh

METHOD 2: npm
  → cd reconcilia-app
  → npm run dev


┌───────────────────────────────────────────────────────────────┐
│  🔐 LOGIN CREDENTIALS                                        │
└───────────────────────────────────────────────────────────────┘

   Email:    test@example.com
   Password: password123

   URL: http://localhost:5173


┌───────────────────────────────────────────────────────────────┐
│  🌐 WSL: ACCESSING FROM WINDOWS                              │
└───────────────────────────────────────────────────────────────┘

   1. Start app in WSL terminal
   2. Open Windows browser (Chrome/Edge/Firefox)
   3. Go to: http://localhost:5173


┌───────────────────────────────────────────────────────────────┐
│  ⛔ STOPPING THE APP                                         │
└───────────────────────────────────────────────────────────────┘

   Press Ctrl + C in the terminal


┌───────────────────────────────────────────────────────────────┐
│  🔧 TROUBLESHOOTING                                          │
└───────────────────────────────────────────────────────────────┘

Problem: Permission denied
Fix:     chmod +x setup.sh

Problem: sudo not found (WSL)
Fix:     wsl -u root
         usermod -aG sudo $USER
         (restart WSL)

Problem: Port already in use
Fix:     lsof -i :5173
         kill -9 <PID>

Problem: Can't access from Windows (WSL)
Fix:     Make sure using http://localhost:5173
         Check Windows Firewall

Problem: npm install fails
Fix:     cd reconcilia-app
         rm -rf node_modules package-lock.json
         npm cache clean --force
         npm install


┌───────────────────────────────────────────────────────────────┐
│  📱 SYSTEM REQUIREMENTS                                      │
└───────────────────────────────────────────────────────────────┘

   Ubuntu:
   ✓ Version 20.04 LTS or newer
   ✓ 4GB RAM (8GB recommended)
   ✓ 1GB free storage
   ✓ Internet connection (for setup)
   ✓ sudo privileges

   WSL:
   ✓ Windows 10 (version 2004+) or Windows 11
   ✓ WSL 2 recommended
   ✓ Ubuntu from Microsoft Store


┌───────────────────────────────────────────────────────────────┐
│  📂 WSL FILE LOCATIONS                                       │
└───────────────────────────────────────────────────────────────┘

   Windows files from WSL:
   → cd /mnt/c/Users/YourName/

   WSL files from Windows:
   → \\wsl$\Ubuntu\home\YourName\

   Reconcilia location:
   → ~/reconcilia-app


┌───────────────────────────────────────────────────────────────┐
│  🔄 UPDATING RECONCILIA                                      │
└───────────────────────────────────────────────────────────────┘

   cd reconcilia-app
   git pull origin main
   npm install
   ./start-reconcilia.sh


┌───────────────────────────────────────────────────────────────┐
│  🗑️  UNINSTALLING                                            │
└───────────────────────────────────────────────────────────────┘

   Remove app:
   rm -rf ~/reconcilia-app

   Remove dependencies (optional):
   sudo apt-get remove nodejs git
   sudo apt-get autoremove


┌───────────────────────────────────────────────────────────────┐
│  ⚙️  WHAT GETS INSTALLED                                    │
└───────────────────────────────────────────────────────────────┘

   • Git         (Version control)
   • Node.js     (v18+ JavaScript runtime)
   • npm         (Package manager)
   • Reconcilia  (The app)


┌───────────────────────────────────────────────────────────────┐
│  💡 USEFUL COMMANDS                                          │
└───────────────────────────────────────────────────────────────┘

   Check Node.js version:
   → node --version

   Check npm version:
   → npm --version

   Check Git version:
   → git --version

   View running processes on port:
   → lsof -i :5173

   Restart WSL (in Windows PowerShell):
   → wsl --shutdown


┌───────────────────────────────────────────────────────────────┐
│  🎯 ONE-LINE INSTALL                                         │
└───────────────────────────────────────────────────────────────┘

   bash <(curl -fsSL https://raw.githubusercontent.com/\
   khalidmihlar/reconcilia-app/main/setup-reconcilia-ubuntu.sh)


═══════════════════════════════════════════════════════════════
                PRINT THIS AND KEEP IT HANDY! 📋
═══════════════════════════════════════════════════════════════


                    WSL-SPECIFIC TIPS

• WSL = Linux running on Windows
• localhost in WSL = localhost in Windows
• Access app from Windows browser
• Files are shared between Windows and WSL
• Use Windows browser for better performance
• WSL terminal stays open while app runs


═══════════════════════════════════════════════════════════════
